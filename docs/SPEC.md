# tganki — функциональная спецификация

Документ описывает поведение бота на уровне «что видит пользователь и что при этом происходит».
Концепция и обоснования — в `CONCEPT.md`. Здесь — правила, по которым пишется код.

Маркировка объёма: **[M1]** — MVP, **[M2]** — после подтверждения retention, **[M3]** — потом.
Пункты с пометкой **[?]** — открытые решения, их нужно принять до кодирования.

---

## 0. Сущности и термины

| Термин | Что это |
|---|---|
| Пользователь | Один Telegram-аккаунт. Личный чат с ботом. Группы — [M3]. |
| Языковая пара | `lang_from → lang_to`: учим слова `lang_from`, переводы на `lang_to`. У пользователя есть **основная пара** (для `/add` и каталога), но деки могут быть с любыми парами. |
| Дека | Набор заметок одной языковой пары. Виды: `builtin` (наша, общая для всех), `user` (личная), `shared` [M2] (личная, опубликованная по ссылке). |
| Заметка (note) | Единица контента: слово, перевод, транскрипция, пример, перевод примера, теги. Хранится **один раз**, даже если деку учат 10 000 человек. |
| Карточка (card) | Заметка × режим (`recognition`: слово → перевод, `recall`: перевод → слово) × пользователь. Несёт состояние FSRS. Создаётся лениво — когда впервые попадает в сессию. |
| Подписка на деку | Связь пользователь ↔ дека с настройками: сколько новых в день, какие режимы. Для своих дек подписка создаётся автоматически. |
| Сессия | Одна серия повторений: очередь карточек, одно редактируемое сообщение, итог. У пользователя не больше одной активной. |
| Оценка | Одна из четырёх: Снова / Трудно / Хорошо / Легко (FSRS: 1–4). |
| День пользователя | Сутки в таймзоне пользователя с границей в **04:00** (как в Anki). |

---

## 1. Онбординг `/start` [M1]

Цель: за ≤ 6 тапов довести до первой карточки. Никаких текстовых вводов, кроме одного опционального.

```
/start
 └─ Шаг 1. Язык интерфейса
      Определяем по `language_code`: ru/uk → как есть, всё остальное → en.
      Сообщение: «Привет! Я помогу учить слова по карточкам. Язык интерфейса: Русский»
      Кнопки: [Русский] [English] [Українська]        ← [?] набор языков интерфейса
 └─ Шаг 2. Какой язык учим
      Кнопки: [🇬🇧 English] [🇩🇪 Deutsch] [🇪🇸 Español] [🇬🇪 ქართული] [Другой…]
      «Другой…» → просим написать название языка текстом → ищем в статическом списке ~40 языков (названия ru/en/родные, коды).
 └─ Шаг 3. На какой язык переводить
      «На какой язык переводить? Интерфейс у тебя на русском — обычно переводят на него.»
      Кнопки: [🇷🇺 Русский] [🇬🇧 English] [🇺🇦 Українська] [Другой…]
      Ничего не выбрано заранее: язык интерфейса — только подсказка в тексте.
      «Другой…» → тот же статический список языков, что и на шаге 2.
      Сохраняем `lang_to`. Смена языка интерфейса (здесь или в настройках) `lang_to` больше не трогает.
 └─ Шаг 4. Уровень
      Кнопки: [Начинаю (A1)] [Немного знаю (A2)] [Средний (B1)] [Не знаю]
      «Не знаю» → A2. Для языков без встроенных дек шаг пропускаем.
      Для грузинского добавляется вариант [Не читаю по-грузински] → сначала дека «Грузинский алфавит», потом A1.
      Подписываем на стартовую деку уровня. Создаём личную деку «Мои слова · EN».
 └─ Шаг 5. Время
      «Сейчас у тебя 19:42?» (считаем по предположению: ru/uk → Europe/Kyiv или Moscow, en → UTC)
      Кнопки: [Да] [Нет]
      «Нет» → «Напиши, сколько у тебя сейчас времени, например 21:15» → считаем смещение,
      округляем до 30 минут, сохраняем как фиксированное смещение (например «+03:00»).
      Точную таймзону с учётом перехода на летнее время получим из Mini App [M2].
 └─ Шаг 6. Напоминание
      «Когда напоминать?» Кнопки: [🌅 08:00] [☀️ 13:00] [🌙 20:00] [Не надо]
 └─ Шаг 7. Первая сессия
      «Готово. Попробуем? 5 карточек, одна минута.» Кнопки: [▶️ Поехали] [Позже]
      «Поехали» → сессия из 5 новых карточек стартовой деки.
```

Правила:
- Повторный `/start` уже настроенного пользователя → главное меню, онбординг не повторяется.
- `/start deck_<publicId>` → сразу подписка на деку (если пользователь новый — сначала онбординг, деку подписываем в конце). 
- `/start ref_<code>` [M2] → реферальная связь.
- Состояние онбординга хранится в БД (`users.onboarding_step`), не в памяти процесса — переживает рестарт.

---

## 2. Главное меню [M1]

Показывается после `/start`, `/menu`, по кнопке «Меню» и по завершении сессии.

```
📚 На сегодня: 12 повторений · 10 новых       🔥 7 дней
Активные деки: English Top 1000 · A2, Мои слова · EN

[▶️ Учить (22)]
[➕ Добавить слово]  [📖 Деки]
[📝 Слова из текста]                          ← только при включённой генерации (§4.3)
[📊 Статистика]      [⚙️ Настройки]
```

Команды (регистрируются через `setMyCommands` для каждого языка интерфейса):
`/learn` `/add` `/decks` `/stats` `/settings` `/undo` `/pro` `/help` `/paysupport` (обязателен по правилам Telegram Stars).
`/extract` (§4.3) работает, но в списке `setMyCommands` его нет: без ключа он умеет только извиниться.

Также кнопка-меню Telegram (`setChatMenuButton`) → в [M2] открывает Mini App, в [M1] — команды.

---

## 3. Сессия повторения [M1]

### 3.1. Сборка очереди

Вход: пользователь, необязательно `deck_id` (учить одну деку). Без `deck_id` — все подписанные деки вперемешку.

1. **Повторения**: все карточки пользователя со `due ≤ now`, не приостановленные, из выбранных дек. Сортировка: по `due` по возрастанию (самые просроченные — первыми). Берём **не больше `SESSION_REVIEW_CAP = 20`** [?]. Остальные остаются на «следующий заход».
2. **Новые**: `лимит_новых_сегодня − уже_введено_сегодня`, где
   - лимит = сумма по декам (`user_decks.new_per_day` или `users.daily_new_limit`, по умолчанию 10),
   - «введено сегодня» = карточки, **получившие сегодня первую оценку** (строка `review_logs`
     со `state_before = New` в текущем дне пользователя). Карточка, которой показали только
     знакомство (§3.2) и которую так и не оценили, лимит не тратит: она остаётся `New`, приходит
     в следующую сессию и списывается с лимита в тот день, когда её действительно оценят.
     Считать по `cards.introduced_at` было бы хуже: сессия, брошенная сразу после десяти
     знакомств, сожгла бы весь дневной лимит и не выдала бы взамен ни одной новой карточки.
   Новые берутся из заметок, у которых ещё нет карточки этого пользователя в этом режиме, в порядке `notes.position`. Встроенные деки отсортированы по полезности внутри каждой части речи и **перемешаны между частями речи** (`scripts/interleave_decks.py`), чтобы первые новые карточки не были сплошными местоимениями и вопросительными словами. При нескольких деках — по кругу (round-robin), чтобы одна дека не съела весь лимит.
   **Защита от снежного кома**: если просроченных повторений > 100, новые в сессию не добавляем и показываем подсказку «Сначала разгребём повторения».
3. **Порядок показа**: сначала все повторения, затем новые. [?] Альтернатива — перемешать новые между повторениями по одной на каждые 3 повторения.
4. Если очередь пуста → «На сегодня всё! Следующие карточки: завтра в 08:00 (14 шт.)». Кнопки: [➕ Ещё 5 новых] [Меню]. «Ещё 5 новых» — разово превышает лимит (как в Anki «custom study»).

### 3.2. Режимы карточек

| Режим | Вопрос | Ответ |
|---|---|---|
| `recognition` | слово, транскрипция | перевод, пример, перевод примера |
| `recall` | перевод | слово, транскрипция, пример |
| `choice` [M1] | слово + 4 варианта перевода (3 дистрактора — из той же деки, того же типа) | оценка автоматически: верно → Хорошо, неверно → Снова; в обоих случаях показывается экран ответа со строкой «✅ Верно» или «❌ Неверно. Правильно: …», переводом и примером, и кнопка «Дальше» |
| `typed` [M2] | перевод, поле ввода | сравнение с учётом регистра/диакритики/1 опечатки; верно → Хорошо, почти → Трудно, нет → Снова |

По умолчанию у деки включён только `recognition`. `recall` включается в настройках деки — тогда у каждой заметки две независимые карточки (как в Anki «forward + reverse»). Чтобы обратная карточка не приходила в тот же день, что и прямая, новая `recall`-карточка становится доступной через 1 день после первого показа `recognition`.

#### Лестница показов новой карточки

Новое слово не угадывают: сначала его показывают целиком, и только потом спрашивают. Каждая новая
карточка проходит три ступени.

1. **Знакомство.** Первый показ новой карточки (`state = New`, `reps = 0`, в этой сессии её ещё не
   показывали) — экран-презентация без вопроса: слово, транскрипция, перевод, пример и его перевод
   (§3.3). Кнопки: «✅ Знаю» (§3.7) и «📖 Не знаю», плюс ✏️ и «⏸ Закончить». **«Не знаю» — не
   оценка**: `review_logs` не пишем, FSRS не трогаем, счётчики сессии и стрик не меняем. В карточку
   пишется `cards.introduced_at = now`, элемент очереди помечается `introduced`, и карточка
   возвращается в очередь через 60 секунд (§3.4); в аналитику пишется событие `card_introduced`.
2. **Узнавание.** Когда карточка вернулась и у неё всё ещё `reps = 0` — первый настоящий опрос:
   «выбор из четырёх» (если настройка `new_card_style = choice` и в деке нашлись три дистрактора),
   иначе обычный «Показать ответ». Ровно один показ на карточку.
3. **Вспоминание.** Начиная с `reps ≥ 1` — обычный поток: «👁 Показать ответ» и четыре оценки.

Знакомство случается **один раз за всю жизнь карточки**, а не один раз за сессию: момент показа
хранится в карточке (`cards.introduced_at`), и пока он не пуст, экран-презентации больше нет.
Поэтому карточку, которую показали во вчерашней сессии и так и не оценили — нажали «Закончить»,
сессию бросили, вернувшаяся копия не успела прийти, — сегодня открываем сразу ступенью узнавания
(`introduced_at` заполнен, `reps = 0`): «выбор из четырёх», а при `new_card_style = reveal` —
«Показать ответ». Внутри сессии тот же факт продублирован флагом `introduced` на элементе очереди
и на копии, которую кладёт возврат: так копия не показывает знакомство второй раз.
`introduced_at` не чистит ничто, кроме сброса прогресса (`/admin reset` удаляет карточки целиком):
ни оценка, ни отмена (`/undo`) — знакомство действительно было. «Ещё 5 новых» (§3.1) и «Учить
сейчас» (§4.1) идут по той же лестнице. Отмена (`/undo`) оценки, шедшей сразу за знакомством,
возвращает ступень узнавания, а не знакомство.

**«Выбор из четырёх» — не отдельный режим, а подача `recognition`** на ступени узнавания.
Экран — в §3.3.

- **Когда.** Настройка `users.new_card_style = choice` («Новые карточки», §8, по умолчанию
  включена), карточка `recognition`, карточка ещё ни разу не оценена (`reps = 0` — ступень
  узнавания, сразу после знакомства), и в деке нашлись три подходящих дистрактора (значит, в деке
  хотя бы 4 заметки). Иначе — обычный «Показать ответ». При `PRO_ENABLED` подача доступна только Pro (§9.1): бесплатному пользователю
  строка настройки показывается с 🔒, а карточки идут обычным потоком.
- **Дистракторы.** Заметки той же деки: сначала с той же первой меткой (часть речи), среди них —
  с самым близким по длине переводом; если таких не хватает, добираем любыми заметками деки.
  Варианты с тем же (нормализованным) переводом, что у правильного ответа или у другого варианта,
  отбрасываются — две одинаковые кнопки сделали бы вопрос неотвечаемым.
- **Стабильность.** Четыре варианта перемешиваются детерминированно по (id сессии, id карточки),
  их `note_id` записываются в элемент очереди (`sessions.queue[i].choice.noteIds`). Поэтому возврат
  к сессии, повторный рендер и запасная отправка нового сообщения (48 ч, §3.4) задают тот же вопрос
  в том же порядке, а тап по варианту проверяется по сохранённым id, а не по тексту кнопки.
- **Оценка.** Обычная, по §3.6: `review_logs`, FSRS, стрик, возврат в очередь по шагам обучения,
  в итоге сессии — обычное повторение. `/undo` откатывает и автоматическую оценку.
- **Мимо.** Оценка «Снова» уже применена, поэтому на экране ответа вместо четырёх кнопок оценки
  одна «▶️ Дальше» (плюс «↩️ Отменить» и «⏸ Закончить»): карточка вернётся сама, шагом обучения.
  Кнопки ✏️ там нет — очередь уже сдвинулась, и меню действовало бы над следующей карточкой.
- **Побег.** Кнопка «👁 Показать ответ» на экране вариантов уводит на обычный поток с четырьмя
  оценками.

### 3.3. Экраны

Одно сообщение, редактируется на месте. Ничего лишнего в чат не пишем.

**Знакомство** (§3.2) — первый показ новой карточки, вопроса нет
```
English Top 1000 · A2                          12 / 25
🆕 новое слово
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
reluctant
/rɪˈlʌktənt/
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
неохотный, сопротивляющийся
She was reluctant to go.
Она не хотела идти.

[ ✅ Знаю ]  [ 📖 Не знаю ]          ← «Знаю» см. §3.7
[ ✏️ ]  [ ⏸ Закончить ]
```
Транскрипция здесь показывается и при настройке «только в ответе»: этот экран и есть сторона
ответа. Скрыта она только при «не показывать». Кнопок «Показать ответ» и «Пропустить» на этом
экране нет; устаревший тап по «Показать ответ» просто перерисовывает знакомство.

**Вопрос**
```
English Top 1000 · A2                          12 / 25
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
reluctant
/rɪˈlʌktənt/

[ 👁 Показать ответ ]
[ ✅ Знаю ]                          ← только для новых карточек, см. §3.7
[ ✏️ ]  [ ⏭ Пропустить ]  [ ⏸ Закончить ]
```
Для новых карточек — метка «🆕 новое» под заголовком.

**Вопрос, «выбор из четырёх»** (§3.2) — вместо «Показать ответ» на первых показах новой карточки
```
English Top 1000 · A2                          12 / 25
🆕 новое
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
reluctant
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Что это значит?

[ 1 · неохотный ]                    ← один вариант в строке, во всю ширину
[ 2 · упрямый ]                        (длинные переводы обрезаются до ~40 символов)
[ 3 · довольный ]
[ 4 · усталый ]
[ 👁 Показать ответ ]                ← обычный поток
[ ✅ Знаю ]                          ← только для новых карточек, см. §3.7
[ ✏️ ]  [ ⏭ Пропустить ]  [ ⏸ Закончить ]
```
Прогноз интервалов здесь не показывается — оценивать ещё нечего.

**Ответ после промаха** (§3.2)
```
❌ Неверно. Правильно: неохотный
English Top 1000 · A2                          12 / 25
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
reluctant
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
/rɪˈlʌktənt/
неохотный, сопротивляющийся
She was reluctant to go.

[ ▶️ Дальше ]
[ ↩️ Отменить ]  [ ⏸ Закончить ]
```

**Ответ**
```
reluctant
/rɪˈlʌktənt/
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
неохотный, сопротивляющийся
She was reluctant to go.
Она не хотела идти.

[ Снова ]  [ Трудно ]  [ Хорошо ]  [ Легко ]
   <1м        5м         10м        4д
[ ↩️ Отменить ]  [ ✏️ ]  [ ⏸ ]
```
Интервалы под кнопками — реальный прогноз FSRS для этой карточки (`previewIntervals`).

**Итог сессии**
```
✅ Сессия завершена · 25 карточек · 4 мин

Снова 3 · Трудно 5 · Хорошо 14 · Легко 3
Точность: 88 %      Новых выучено: 8
🔥 Стрик: 8 дней

Осталось на сегодня: 15 повторений
[ ▶️ Продолжить (15) ]   [ Меню ]
```

### 3.4. Переходы и правила оценки

- Оценка сохраняется **немедленно** при нажатии: обновляем карточку, пишем `review_logs`, двигаем позицию сессии. Брошенная сессия не теряет ничего.
- **Знакомство — не оценка** (§3.2). «📖 Не знаю» ничего не пишет в `review_logs` и не трогает FSRS,
  стрик и счётчики сессии: карточке проставляется `introduced_at` (один раз за её жизнь, повторные
  нажатия отметку не двигают), элемент очереди помечается `introduced`, и карточка кладётся в конец
  очереди с пометкой «не раньше чем через 60 секунд». Вернётся она обычным порядком — если других карточек не
  осталось, показываем досрочно. Этот возврат не тратит одну из трёх попыток `requeues`: лимит
  считает только возвраты по шагам обучения. В итоге сессии знакомства не видны: `reviewed`,
  «Новых выучено» и точность считают только оценки.
- Карточка, получившая оценку с коротким интервалом (< 20 минут — это шаги обучения FSRS: Снова → 1 мин, Трудно → 5 мин, Хорошо на новой → 10 мин), **возвращается в текущую очередь по времени**: она встаёт в конец с пометкой «не раньше чем в HH:MM» и показывается, только когда её шаг реально истёк. Если других карточек не осталось, показываем её досрочно (как «learn ahead» в Anki), чтобы не заставлять ждать. За одну сессию карточка возвращается не больше 3 раз, дальше ждёт следующей сессии. Так работают «learning steps» без таймеров.
- Карточка с интервалом ≥ 1 день покидает сессию.
- `Пропустить` → карточка уходит в конец очереди без оценки (один раз за сессию; второй пропуск = «отложить до завтра», bury).
- `Закончить` → сессия помечается `finished`, показываем итог по уже сделанному.
- `Отменить` (`/undo`) → откатываем последнюю оценку по снимку из `review_logs` (стабильность, сложность, due, состояние, счётчики), удаляем строку лога, возвращаем карточку как текущую. Глубина — 1 шаг. Работает только для последней оценки в активной сессии.
- **Двойной тап**: в callback data — номер позиции в очереди; если он не совпадает с текущей позицией сессии → `answerCallbackQuery("Уже оценено")`, ничего не делаем.
- `/learn` при активной сессии → продолжаем её: пытаемся отредактировать старое сообщение; если Telegram отвечает «message not found» или сообщение старше 48 ч → отправляем новое и запоминаем его `message_id`.
- Сессия без активности 12 часов считается `abandoned` при следующем `/learn`; новая собирается заново.
- **Пиявки (leech)**: 8 оценок «Снова» на карточке → после сессии сообщение «Слово X никак не даётся. [Приостановить] [Переписать карточку] [Оставить]».

### 3.5. Действия с карточкой (кнопка ✏️)

Меню поверх текущей карточки:
- «✅ Уже знаю» — слово выключается во всех деках, см. §3.7. Доступно для любой карточки,
  не только новой.
- «Приостановить» — карточка исключается из очередей (`suspended`), в декe остаётся.
- «Отложить до завтра».
- «✨ Дополнить» — только для своих заметок, у которых нет транскрипции или примера, и только когда
  включена генерация (§4.1a): заполняет пустые поля, чужое содержимое не трогает.
- «Изменить перевод/пример» [M2] — для своих заметок; для встроенных — создаётся личная копия заметки (override).
- «Сообщить об ошибке» — для встроенных дек: флаг в таблицу `note_reports`, мы смотрим руками.
- «Удалить» — только для заметок в своих деках.

### 3.6. Что обновляет каждая оценка

```
input:  card, rating ∈ {1,2,3,4}, now
FSRS:   next = scheduler.next(card, now, rating)
write:  review_logs ← снимок card ДО + rating + now
        cards ← next.card (state, stability, difficulty, due, last_review, reps, lapses, ...)
        sessions.position += 1; sessions.stats[rating] += 1
        streak ← touch(user, now)         // см. §6
        events ← "review" {rating, is_new}
```
Все записи в одной транзакции. Целевая задержка от тапа до отредактированного сообщения — < 500 мс.

### 3.7. Известные слова и дубликаты [M1]

Встроенные деки самодостаточны и **намеренно пересекаются**: одно и то же слово может быть
и в A1, и в A2. Правило простое: **одно и то же слово пользователь не учит дважды и может
выключить его одним тапом**.

**Нормализованное слово** = `notes.front` после NFC, схлопывания пробелов, обрезки и приведения
к нижнему регистру. В SQL это `frontNorm()` (`src/db/sql.ts`), под него есть выражательный
индекс `notes_front_norm_idx`.

**Что исключается из новых карточек** (одинаково в сборке очереди, в счётчике «Новых» на карточке
деки, в счётчиках меню и статистики):

1. заметка, у которой пара (`decks.lang_from`, нормализованный `front`) есть в `known_words`
   этого пользователя;
2. заметка, для слова которой у пользователя **уже есть карточка по другой заметке** того же
   языка обучения — в любом состоянии, приостановленная или нет.

Правило 2 означает, что подписка на новую деку не приносит повторно то, что уже учится;
правило 1 — что выключенное слово не вернётся даже в деках, на которые пользователь подпишется
потом.

Эти два правила — про **очередь**, а не про знание. «Слова из текста» (§4.3) считает знанием
только строку в `known_words`, карточку с `reps > 0` и заметку в своей деке: слово, которое просто
лежит в подписанной встроенной деке и до которого пользователь ещё не дошёл, там не отбрасывается,
а предлагается взять в сессию.

**«✅ Знаю»** (кнопка на вопросе и на ответе новой карточки и пункт «Уже знаю» в меню ✏️):

```
known_words ← (user, deck.lang_from, нормализованный front)
cards       ← suspended = true, suspended_reason = 'known'
              для этой карточки и всех карточек пользователя с тем же словом в других деках
events      ← "word_known"
очередь     ← сдвигается дальше без оценки: ни review_logs, ни stats.reviewed, ни стрика
```
Тост: «Отключил «X» во всех деках». Двойной тап отсекается по номеру позиции, как у оценки.

`cards.suspended_reason` (`known` | `manual` | `leech` | `duplicate`) — только для отчётности,
источник правды — булев `suspended`.

Вернуть выключенное можно на карточке деки: «↩️ Вернуть отключённые» (см. §5.1).

---

## 4. Добавление слов

### 4.1. Одно слово [M1] — ручной ввод

Базовый путь, он же fallback для §4.1a: работает всегда, когда генерация выключена или не удалась.

Триггер: `/add`, кнопка «Добавить слово» или **просто текст в чат** (не команда, ≤ 40 символов, ≤ 3 слов).

```
пользователь: reluctant
бот:          Перевод для «reluctant»?
              Подсказка: можно сразу писать «слово - перевод».
              [ 📚 В другую деку ]  [ ✖ Отмена ]
пользователь: неохотный
бот:          ✅ Добавлено в «Мои слова · EN»
              reluctant — неохотный
              [ ➕ Ещё слово ]  [ Меню ]
```

Правила:
- Форма «в одно сообщение»: `слово - перевод`, `слово — перевод`, `слово – перевод` или через табуляцию → заметка создаётся сразу, без вопроса.
- Несколько строк такого вида (до 100) → пакетное добавление: «Добавлено 12 слов, пропущено 2 дубликата».
- `front` = введённое слово, trim и схлопывание пробелов; регистр не меняем.
- При генерации словарная форма подбирается только для одиночных слов; фраза из двух и более слов на изучаемом языке сохраняется как введена (без завершающей точки), переводится целиком и используется в примере.
- **Дубликаты**: если у пользователя уже есть заметка с таким `front` в любой его деке → «Уже есть в «Мои слова · EN»: reluctant — неохотный. [Добавить всё равно]». Если слово есть во встроенной деке, на которую он подписан → «Есть в «English Top 1000 · A2»: reluctant — неохотный. [Учить сейчас] [Добавить всё равно]». Показываем название деки и перевод; номер позиции пользователю ничего не говорит и не выводится. «Учить сейчас» создаёт карточку немедленно вне очереди новых.
- «Добавить всё равно» никогда не возвращает к пустому вопросу: карточка, которая уже есть в черновике (сгенерированная или введённая парой), показывается превью §4.1a, а если модель ещё не вызывалась — вызывается сейчас. Сам дубликат при этом больше не проверяется: пользователь его уже видел.
- Транскрипция и пример у ручных карточек пустые. Если генерация включена, после сохранения пары
  `слово - перевод` бот дозаполняет их фоном (§4.1a), а вручную это делает кнопка «✨ Дополнить» в меню ✏️.
- Целевая дека по умолчанию — «Мои слова · <язык>» основной пары; «В другую деку» показывает список своих дек.

### 4.1a. AI-заполнение карточки [M1, включено при наличии ключа]

Включается наличием `OPENROUTER_API_KEY`. Модель — `LLM_MODEL` (по умолчанию `google/gemini-3.7-flash`,
выбрана прогоном `scripts/llm-eval`, отчёт в `scripts/llm-eval/results/railway-1/REPORT.md`).
Контракт слоя — `src/llm/types.ts`, реализация — `src/llm/{openrouter,prompt,generator,cache}.ts`.

Одно слово (≤ 40 символов, ≤ 3 слов, без пары через тире/таб):

```
пользователь: reluctant
бот:          ⏳ Подбираю перевод…                    ← это же сообщение редактируется в превью
бот:          reluctant  /rɪˈlʌktənt/ · прил.
              неохотный, сопротивляющийся
              She was reluctant to go. — Она не хотела идти.
              → в деку «Мои слова · EN»
              [ ➕ Добавить ]  [ 📚 В другую деку ]
              [ ✏️ Свой перевод ]  [ ✖ ]
```

Правила:
- **Дубликаты проверяются дважды**: по введённому слову — *до* генерации (слово, которое уже есть,
  не стоит ни одного запроса к модели), и по каноническому `front` — *после*, если он отличается
  от введённого. Экран дубликата тот же, что в §4.1.
- **Направление** берётся из `detectedLang`: если он равен `lang_to`, пользователь написал на родном
  языке, и сгенерированный `front` — это иностранное слово («неохотный» → `reluctant`).
- «Добавить» сохраняет `front/back/transcription/example/example_tr` в выбранную деку.
  `pos` показывается в превью, но не хранится (колонки нет).
- «Свой перевод» спрашивает текст и сохраняет его как `back`, оставляя сгенерированные
  транскрипцию и пример.
- «В другую деку» меняет целевую деку прямо в превью, не теряя карточку.
- **Ошибка генерации** (таймаут, недоступность, невалидный ответ, `detectedLang: "und"` на мусорном
  вводе) → обычный вопрос §4.1 плюс строка «Не удалось подобрать перевод автоматически».
  Генерация никогда не блокирует добавление.
- `слово - перевод` (одной строкой и пачкой) остаётся ручным. После сохранения одиночной пары
  транскрипция и пример дозаполняются **фоном**: ошибки игнорируются, заполняются только пустые поля.
- **Кэш** — таблица `generated_cache`, ключ `card:v1:<lang_from>:<lang_to>:<слово>` (NFC, trim,
  схлопывание пробелов, нижний регистр), общий на всех пользователей. Промах дополнительно
  сохраняет карточку под ключом канонического `front`, поэтому «неохотный» и «reluctant» дальше
  отвечают из одной записи. Невалидный payload считается промахом.
- **Лимит** (§9.1): 10 генераций за учебный день на бесплатном плане при `PRO_ENABLED=true`.
  Считаются только реальные вызовы модели (`word_generated` с `cached: false`); попадания в кэш
  бесплатны. Лимит исчерпан → ручной ввод плюс строка про автоподбор.
- События: `word_generated { cached, latencyMs, model, langFrom, langTo }` и
  `word_generation_failed { reason }`.
- **Бюджет ответа**: `max_tokens: 4000` (как в прогоне eval) и `LLM_REASONING_EFFORT`, если он задан.
  Дефолта клиента (600) рассуждающей модели не хватает: JSON приходит обрезанным. Если
  провайдер вернул `finish_reason: "length"`, это попадает в лог и в текст ошибки — обрезку
  видно, а не «невалидный JSON».
- Стоимость выбранной модели по прогону — около $2 на 1000 новых слов до кэша.

### 4.2. Пакетная вставка [M2]

Текст из нескольких строк вида `word - перевод` / `word — перевод` / `word\tперевод` → парсим без LLM, показываем «Распознано 12 пар. [Добавить все] [Показать]». Транскрипцию и примеры догенерируем фоном.

### 4.3. Слова из текста [M1, включено вместе с генерацией]

Работает при том же `OPENROUTER_API_KEY`, что и §4.1a, той же моделью. Реализация:
`src/llm/extractor.ts` (промпт и схема — в `src/llm/prompt.ts`), `src/services/extractService.ts`,
`src/bot/extract.ts`.

Триггер: сообщение, которое не похоже на слово, — **длиннее 40 символов или больше 3 слов**, —
а также **любое пересланное** сообщение с текстом, команда `/extract` и кнопка меню
«📝 Слова из текста» (её видно только при включённой генерации). Состояние ожидания ввода
приоритетнее (§11): текст, которого ждут в другом месте, в разбор не уходит. Пара
`слово - перевод` по-прежнему побеждает: она сохраняется как обычно, без модели.

```
пользователь: <абзац на грузинском>
бот:          🔎 Ищу незнакомые слова…            ← это же сообщение редактируется дальше
бот:          📝 Нашёл 8 слов
              Уже знаешь: 3
              1. ბაზარი — рынок
              2. ხილი — фрукты
              3. გამყიდველი — продавец · в деке «Грузинский Top 500 · A1»
              …
              → в деку «Мои слова · KA»
              [☑ 1] [☑ 2] [☑ 3] [☑ 4] [☑ 5]
              [☑ 6] [☑ 7] [☑ 8]
              [✅ Добавить выбранные (8)]
              [Выбрать все]  [Снять все]
              [📚 Другая дека]  [✖]
пользователь: ✅ Добавить выбранные
бот:          ✅ Добавил 4 новых слова в «Мои слова · KA» и взял 6 из деки
              «Грузинский Top 500 · A1» в ближайшую сессию
              ბაზარი — рынок, ხილი — фрукты, …
              [▶️ Учить новые]  [Меню]
```

Правила:
- **Что уходит в модель**: текст без ссылок (`https://…`, `www.…`, `example.com/…` вырезаются),
  обрезанный до **3000 символов** — про обрезку говорим строкой «Текст длинный — взял первые
  3000 символов». Вместе с текстом: `langFrom`, `langTo` и **уровень** — самый высокий уровень
  встроенной деки изучаемого языка, на которую подписан пользователь (A0 → A1), по умолчанию A2.
- **Ответ модели**: `{ detectedLang, words: [{ front, back, inText }] }`, json_schema strict,
  `max_tokens: 4000`, **без кэша** — двух одинаковых текстов не бывает. Не больше 25 слов,
  по убыванию полезности, без имён собственных, чисел и слов ниже уровня, без повторов.
  Пост-обработка: схлопывание пробелов, отбрасывание пустых и повторов, отбрасывание слов
  **не в письменности `langFrom`** (для `ka` латиница отбрасывается), кап 25.
- **Не тот язык**: `detectedLang == langTo` → «Это твой родной язык (русский). Нужен текст на языке,
  который ты учишь: грузинский.»; `detectedLang` не равен ни `langFrom`, ни `langTo` → «Похоже, это
  не грузинский, а английский…». В обоих случаях список не показываем.
- **Три корзины.** Каждое найденное слово (по нормализованному `front`, §3.7) попадает ровно в
  одну — это `notesRepo.classifyFronts`, два запроса на весь список:
  1. **знает** — есть строка в `known_words`; **или** у пользователя есть карточка на заметку с
     таким же словом и `reps > 0` (хоть раз оценил; нетронутая карточка в состоянии `new` не в
     счёт); **или** такая заметка лежит в **своей** деке пользователя — это настоящий дубликат.
     Такие слова в список не попадают: их только считаем строкой «Уже знаешь: N».
  2. **есть в деке** — заметка с этим словом лежит в деке, на которую пользователь **подписан**
     (встроенной), и он её ещё не начал (карточки нет или у неё `reps = 0`). Подписка на деку из
     300 слов, до которых пользователь не дошёл, — не знание: такое слово показываем в списке с
     пометкой «· в деке «Грузинский Top 500 · A1»» и запоминаем `noteId`.
  3. **новое** — всё остальное.
- **Ничего не осталось**: если нашлось только известное — «Все 10 найденных слов ты уже знаешь: …»
  (перечисляем до 10 слов, чтобы было видно, что вообще нашлось); если модель не нашла ничего —
  «Незнакомых слов не нашёл.» Формулировка «Ничего не добавил. Пропустил N (уже есть).» в разборе
  текста не используется.
- **Чек-лист** живёт в `pending_payload` вместе с ревизией черновика (§4.1): переключение галочки,
  «Выбрать все», «Снять все» и смена деки перерисовывают то же сообщение и ревизию не меняют.
  Всё выбрано по умолчанию. Callback-и: `x:t:<rev>:<i>`, `x:add:<rev>`, `x:all:<rev>`,
  `x:none:<rev>`, `x:decks:<rev>`, `x:deck:<rev>:<deckId>`, `x:cancel:<rev>`, `x:learn:<rev>`.
- **Добавление** идёт по корзинам, и итог считает их отдельно: «✅ Добавил 4 новых слова в «Мои
  слова · KA» и взял 6 из деки «Грузинский Top 500 · A1» в ближайшую сессию» (когда деки разные —
  «из твоих дек»).
  - **новое** — карточка через тот же кэшируемый генератор §4.1a, по 3 запроса параллельно.
    `front` и `back` сохраняются **ровно те, что пользователь видел в чек-листе**; от модели
    берутся только транскрипция и пример, и только если она ответила про то же слово. Ошибка
    генерации не мешает: слово сохраняется парой. Дубликаты, появившиеся за это время,
    пропускаются и считаются строкой «N слов уже были у тебя — пропустил.»
  - **есть в деке** — ни генерации, ни своей заметки: сразу материализуем карточку встроенной
    заметки с `due = now`, как «▶️ Учить сейчас» для одного слова (§4.1). Дальше она живёт по
    обычным правилам очереди (§3.1, `notKnownOrDuplicate` §3.7). Бюджет ИИ и лимит заметок такие
    слова не тратят, и смена деки через «📚 Другая дека» на них не влияет — дека выбирается только
    для новых слов.
  - «▶️ Учить новые» открывает сессию по обеим половинам: и по только что созданным заметкам, и по
    поднятым карточкам деки.
- **Бюджет** (§9.1): сам разбор текста — 1 в день на бесплатном плане (`text_extracted` за учебный
  день). Генерация карточек тратит обычный дневной лимит §4.1a; когда он кончается, остальные
  слова не добавляются, и мы пишем сколько именно.
- **Без ИИ**: длинный текст → «Чтобы находить слова в тексте, нужен подключённый ИИ.» Словом такой
  текст не считается, ничего не создаётся.
- **Крайние случаи**: длинное сообщение, от которого после вырезания ссылок не осталось букв →
  «Незнакомых слов не нашёл.»; короткий остаток (≤ 40 символов и ≤ 3 слов) обрабатывается как
  обычное слово §4.1.
- События: `text_extracted { words, dropped, inDeck, model, latencyMs, chars, detectedLang }` при
  каждом состоявшемся вызове модели (в том числе с пустым результатом — он тоже стоил запроса) и
  `text_words_added { n, taken }` (`n` — новые заметки, `taken` — поднятые карточки деки); каждая
  сгенерированная карточка пишет свой `word_generated` с `via: "extract"`.

### 4.4. Из фото [M3], импорт CSV/`.apkg` [M2].

---

## 5. Деки `/decks`

### 5.1. Мои деки [M1]
```
📖 Мои деки

English Top 1000 · A2        🆕 148 · ⏰ 12 · всего 350
Мои слова · EN               🆕 3 · ⏰ 0 · всего 41
[ ➕ Новая дека ]  [ 📚 Каталог ]
```
Каждая дека — кнопка. Пагинация по 8.

**Карточка деки**
```
English Top 1000 · A2
Самые частотные слова уровня A2.
Новых: 148 · На сегодня: 12 · Выучено: 190 · Всего: 350
Новых в день: 10 · Режимы: слово → перевод
Отключено: 3                                  ← только если есть выключенные карточки

[ ▶️ Учить эту деку ]
[ ⚙️ Новых в день ]  [ 🔁 Режимы ]
[ ↩️ Вернуть отключённые ]                     ← только если «Отключено» > 0
[ 🔗 Поделиться ]    [ ➖ Отписаться ]
```
«Новых» считается по тем же правилам, что и очередь: известные и дублирующиеся слова
не учитываются (§3.7). «Отключено» — приостановленные карточки деки, независимо от причины.
«Вернуть отключённые» снимает `suspended` со всех карточек этой деки и удаляет
соответствующие записи `known_words`; подтверждение не спрашиваем.
Для своей деки вместо «Отписаться» — «🗑 Удалить» (с подтверждением; удаляются и прогресс, и заметки).
Отписка от встроенной деки прогресс **не удаляет** — при повторной подписке всё вернётся.

### 5.2. Каталог [M1]
Фильтр по языку обучения (по умолчанию — основная пара). Список встроенных дек с уровнем и размером. 🔒 — Pro-деки. Кнопка [➕ Добавить] → подписка → предложение «Учить сейчас».

### 5.3. Своя дека [M1]
«Новая дека» → название текстом → языковая пара (кнопка «как основная» или выбор) → создана, подписка автоматическая. Добавление слов — через `/add` с выбором «В другую деку».

### 5.4. Поделиться [M1 для builtin, M2 для своих]
Ссылка `https://t.me/<bot>?start=deck_<publicId>`. Для своей деки публикация делает деку `shared`: получатели видят копию заметок **только для чтения**, их прогресс — их собственный; изменения автора видны подписчикам. Экспорт CSV [M2]: `front,back,transcription,example,example_tr,tags`.

---

## 6. Стрик, напоминания, отчёты

### 6.1. Стрик [M1]
- День засчитан, если за «день пользователя» (с 04:00 до 04:00 местного) есть ≥ 1 оценка.
- При каждой оценке: если `streak_last_day == сегодня` → ничего; если `== вчера` → `streak += 1`; если `== позавчера` и есть заморозка → `streak += 1`, заморозка потрачена, пользователь видит «🧊 Заморозка спасла стрик»; иначе `streak = 1`.
- Заморозка: одна, восстанавливается каждый понедельник. Pro [M2]: две.
- Стрик показывается в меню, в итоге сессии и в напоминании.

### 6.2. Напоминание [M1]
- Крон раз в минуту: выбрать пользователей, у которых `reminder_time` в их местном времени совпадает с текущей минутой и сегодня напоминание ещё не отправлялось.
- Если на сегодня есть что учить (просроченные > 0 или доступны новые):
  «📬 На сегодня: 12 повторений и 10 новых · ~6 мин. 🔥 Стрик 7 дней — не потеряй. [▶️ Начать]».
  Если сегодня уже занимался — не шлём.
- Оценка времени: 12 с на карточку.
- Отправка через очередь с троттлингом ≤ 25 сообщений/с.
- Ответ 403 (пользователь заблокировал бота) → `users.blocked_at = now`, напоминания отключаем, метрика оттока. При любом новом сообщении от него `blocked_at` сбрасывается.

#### «Стрик в опасности» [M2]
Тот же крон, та же минутная сверка по местному времени, но в **21:00**. Условия: стрик ≥ 3, за
текущий день пользователя (граница 04:00) нет ни одной оценки, бот не заблокирован, онбординг
закончен, напоминания включены (`reminder_time is not null`), тумблер «Напоминать о стрике» включён
и сегодня напоминание о стрике ещё не отправлялось (`last_streak_nudge_day`).
Если дневное напоминание стоит в ближайшие **3 часа** (то есть между 21:00 и 00:00) — молчим, иначе
получится два пинга подряд.

  «🔥 Стрик 7 дней сгорит через 7 часов. Сегодня ещё ни одной карточки. Хватит и пяти минут.
  [▶️ Начать]»

«Через сколько часов» считается до ближайшей границы 04:00. Если недельная заморозка ещё не
потрачена, в конце добавляется «Заморозка спасёт стрик один раз, но лучше не тратить».
Событие `streak_nudge_sent { streak }`, 403 обрабатывается как у дневного напоминания.

### 6.3. Недельный отчёт [M2]
Понедельник **10:00 местного**, один раз за ISO-неделю (`last_weekly_report_week`, например
«2026-W36»). Кому: активным за последние 14 дней (была хоть одна оценка), не заблокированным, с
включёнными напоминаниями — отдельного тумблера у отчёта нет, он следует за напоминанием.
Отчёт покрывает закончившуюся неделю: от 04:00 понедельника прошлой недели до 04:00 сегодняшнего.

```
📈 Неделя 31 авг – 6 сен
Повторений: 214 · Точность: 86 %
Новых слов: 38 · Выучено (стабильность ≥ 21 д): 12
Дней с занятиями: 5 из 7 · 🔥 Стрик: 9
Самые трудные: reluctant, ყველი, der Tisch
Ближайшая неделя: ~120 повторений
[ ▶️ Учить ]  [ 📊 Статистика ]
```

«Самые трудные» — топ-3 слова по числу «Снова» за неделю. «Выучено» — карточки, которые за эту
неделю пересекли порог зрелости (стабильность ≥ 21 д) и держатся выше него. «Ближайшая неделя» —
карточки, у которых срок наступает в следующие 7 дней.
Если за неделю 0 повторений и 0 новых — отчёт не отправляется; но если на прошлой неделе оценки
были, вместо него уходит короткое «На этой неделе не было занятий. 🔥 Стрик: 9 дней — вернись, пока
он держится». Событие `weekly_report_sent { reviews, newCards }`.

Все три задачи (§6.2 дневное напоминание, «стрик в опасности», §6.3 отчёт) крутятся на одном
минутном тике, с общим троттлингом и общей обработкой 403; тики не накладываются, а отметки
`last_reminded_day` / `last_streak_nudge_day` / `last_weekly_report_week` делают их идемпотентными
после перезапуска.

---

## 7. Статистика `/stats` [M1]

Текст (графики — Mini App [M2]):
```
📊 Статистика

Сегодня: 25 повторений · точность 88 %
За 7 дней: 140 повторений · 52 новых · точность 85 %
🔥 Стрик: 8 дней (рекорд 12)

Карточки: 41 новых · 18 в изучении · 96 на повторении · 34 выучены
Прогноз: завтра 14 · за неделю 78

[ По декам ]  [ Меню ]
```
«Выучены» = стабильность ≥ 21 день (условно «mature»). «В изучении» = состояние learning/relearning.

---

## 8. Настройки `/settings` [M1]

Каждый пункт — кнопка, меняющая значение на месте:
- Язык интерфейса — только тексты бота, `lang_to` не меняет.
- Язык обучения `lang_from` (для `/add` и каталога) — строка «Учу: грузинский».
- Язык перевода `lang_to` — строка «Переводить на: русский», кнопка «🌐 Язык перевода»,
  тот же выбор, что на шаге 3 онбординга ([Русский] [English] [Українська] [Другой…]).
  Личные деки, созданные раньше, сохраняют свой `lang_to`; новые создаются с текущей парой.
- Новых карточек в день (5 / 10 / 20 / 30 / своё число) — глобальный дефолт; переопределяется в деке.
- Время напоминания / выключить.
- Напоминать о стрике: вкл/выкл (§6.2). Показывается рядом со временем напоминания; при выключенных
  напоминаниях читается как «выключено». Недельный отчёт (§6.3) отдельного тумблера не имеет.
- Часовой пояс (снова через «сколько у тебя сейчас времени»).
- Желаемая запоминаемость 0.80 / 0.85 / 0.90 / 0.95 [Pro, M2] — единственный параметр FSRS наружу.
- Показывать интервалы: да/нет.
- Транскрипция: в вопросе и ответе / только в ответе (по умолчанию) / не показывать.
- Новые карточки: выбор из четырёх (по умолчанию) / показать ответ — чем спрашивать новую
  карточку на ступени узнавания, то есть при `reps = 0` (§3.2). Знакомство показывается в обоих
  случаях. При `PRO_ENABLED` строка помечена 🔒 для бесплатного плана.
- Удалить аккаунт и все данные — с подтверждением текстом «УДАЛИТЬ».

---

## 9. Pro и оплата `/pro`

### 9.1. Что ограничено (Free) — рубильник `PRO_ENABLED` [M1 — код с рубильником, включаем позже]

| Ограничение | Free | Pro |
|---|---|---|
| Свои деки | 3 | ∞ |
| Своих заметок всего | 300 | ∞ |
| AI-генераций в день [M1] | 10 | ∞ |
| Добавление из текста [M1] | 1 текст в день | ∞ |
| Встроенные деки | базовые | все, включая 🔒 |
| «Выбор из четырёх» (§3.2) [M1] | нет | да |
| Режим `typed`, аудио [M2] | нет | да |
| Импорт файлов [M2] | нет | да |
| Заморозок стрика | 1 | 2 |
| Желаемая запоминаемость | 0.9 фикс. | настраивается |
| Персональные веса FSRS [M3] | нет | да |

При `PRO_ENABLED=false` все проверки возвращают «разрешено». Так мы кодим гейтинг сразу, но запускаемся бесплатно.

День для генераций — учебный (граница 04:00 в таймзоне пользователя, как у стрика). Счётчик не хранится
отдельной колонкой: это количество событий `word_generated` с `cached: false` за текущий день (§12).
Разбор текста (§4.3) считается так же — по событиям `text_extracted` за учебный день; карточки,
которые пользователь потом добавляет из чек-листа, тратят обычный лимит генераций.

### 9.2. Оплата через Telegram Stars [M2]
- Товары: `pro_month` (подписка, `subscription_period = 30 дней`), `pro_year` (разовый платёж на 365 дней), `pro_lifetime`, `deck_<slug>` (разовый).
- Поток: `/pro` → экран с тарифами → `sendInvoice(currency="XTR")` → `pre_checkout_query` (проверяем, что товар существует; отвечаем `ok` за < 10 с) → `successful_payment` → запись в `payments`, `users.plan`, `plan_until`.
- Подписка: продления приходят как новые `successful_payment` с `is_recurring = true` — продлеваем `plan_until`. Отмена происходит на стороне Telegram; мы просто видим, что `plan_until` прошёл → крон раз в час переводит в `free`.
- `/paysupport` — обязательная команда: контакт для вопросов по платежам и политика возврата (возврат через `refundStarPayment` в течение 14 дней по запросу).
- Подарок Pro другу [M3], партнёрская программа Telegram [M3].

---

## 10. Локализация [M1]

- Все строки — через ключи (`@grammyjs/i18n`, файлы `locales/ru.ftl`, `en.ftl`, …), с плюрализацией.
- Языки интерфейса на старте: ru, en [?]. Добавление языка = один файл.
- Названия встроенных дек и их описания — тоже локализуются (в JSON деки: `title: {ru: …, en: …}`).
- Даты и интервалы форматируются по локали: «через 3 дня», «in 3 days».

---

## 11. Крайние случаи и устойчивость

| Ситуация | Поведение |
|---|---|
| Сообщение сессии удалено пользователем или старше 48 ч | Отправляем новое, старое забываем |
| Telegram вернул 429 | Ждём `retry_after`, повторяем; в напоминаниях — троттлинг заранее |
| Два быстрых тапа по оценке | Второй игнорируется по номеру позиции |
| Пользователь пишет во время онбординга не то, что ждём | Повторяем текущий шаг с подсказкой |
| Текст пришёл, а бот ждал название деки | Состояние ожидания ввода имеет приоритет над «добавить слово»; TTL состояния — 10 минут |
| Текст пришёл, а состояние ожидания ничьё (истекло, осталось от старой версии, экран давно сменился) | Молча забываем состояние и обрабатываем текст как новое слово. Свободный текст никогда не отвечает «Ок, ничего не добавляю» — это ответ только на кнопку ✖ |
| Экран показал черновик (превью или дубликат) | `pending_input` очищается: следующий текст — новое слово, кнопки на экране работают с `pending_payload` |
| Тап по кнопке старого черновика (превью, дубликат, чек-лист §4.3 — экран, после которого пользователь уже отправил что-то новое) | Ничего не меняем: тост «Это старое сообщение, отправь слово ещё раз» и снимаем клавиатуру с того сообщения. Каждый черновик получает растущую ревизию, и она едет в callback-данных каждой его кнопки (`a:g:<rev>`, `x:t:<rev>:<i>`, …). Кнопки, оставшиеся от прошлой версии бота (без ревизии), попадают в ту же ветку |
| Длинный текст, ссылка или пересланное сообщение | §4.3: ищем в нём незнакомые слова. Без `OPENROUTER_API_KEY` — «Чтобы находить слова в тексте, нужен подключённый ИИ» |
| В тексте не тот язык | §4.3 не показывает список: говорим, какой язык увидели, и просим текст на изучаемом |
| Пара «слово - перевод» пришла на вопрос «Перевод для X?» | Побеждает пара: подсказка под вопросом сама предлагает такой формат |
| LLM недоступен, отвечает медленно или вернул мусор | Сообщение «⏳ Подбираю перевод…» редактируется в обычный вопрос §4.1 со строкой «Не удалось подобрать перевод автоматически»; добавление не блокируется |
| Ключа `OPENROUTER_API_KEY` нет | Генерации нет вообще, работает только ручной путь §4.1 |
| Мусорный ввод (эмодзи, ссылка, случайные буквы) | Модель возвращает `detectedLang: "und"`, мы считаем это неудачей и спрашиваем перевод руками |
| Слово на неожиданном языке [M2] | LLM сообщает `detected_lang`; предлагаем «Это французский. Добавить в новую деку FR → RU?» |
| Пользователь заблокировал бота | Помечаем, не шлём напоминания; данные храним 12 месяцев, потом чистим |
| Рестарт сервиса посреди сессии | Всё в БД, сессия продолжается с той же позиции |
| Одно и то же слово в двух подписанных деках | Заметки в разных деках — разные записи, но новой карточкой слово выдаётся один раз: сборка очереди отбрасывает слово, которое уже учится по другой заметке того же языка (§3.7) |

---

## 12. Аналитика [M1]

Таблица `events(user_id, name, props jsonb, at)`. События: `start`, `onboarding_done`, `session_start`, `card_introduced`, `review`, `session_end`, `word_added`, `word_generated`, `word_generation_failed`, `text_extracted`, `text_words_added`, `word_known`, `deck_subscribed`, `deck_created`, `deck_restored`, `reminder_sent`, `reminder_clicked`, `streak_nudge_sent` (`{ streak }`, §6.2),
`weekly_report_sent` (`{ reviews, newCards }`, §6.3), `pro_screen`, `payment`, `blocked`.
`word_generated` несёт `{ cached, latencyMs, model, langFrom, langTo }` — из него же считается дневной
лимит §9.1, поэтому отдельной колонки-счётчика нет. `text_extracted`
(`{ words, dropped, model, latencyMs, chars, detectedLang }`) считает дневной лимит §4.3 и заодно
показывает, сколько слов из текста реально доходит до карточек вместе с `text_words_added { n }`.
Этого достаточно, чтобы посчитать воронку онбординга, D1/D7/D30, длину сессии и конверсию. Дашборд — простые SQL-запросы, `/admin stats` для владельца.

---

## 13. Администрирование [M1 минимум]

`/admin` доступен только `ADMIN_TG_IDS`: сводка (пользователи всего/сегодня, сессий сегодня, оценок сегодня), список жалоб на карточки (`note_reports`), выдать Pro вручную (`/admin pro <tg_id> [дней]`), сбросить прогресс пользователя (`/admin reset <tg_id>`: карточки, история, сессии, известные слова (§3.7), стрик; настройки, подписки и свои слова остаются). Рассылка [M2].

---

## 14. Принятые решения

Решения приняты 2026-09-04 (рекомендованные варианты подтверждены):

1. **Языки интерфейса на старте**: ru + en. Украинский и другие — по одному файлу локали позже.
2. **Языки обучения**: свои слова — любой язык с первого дня (ручной ввод перевода), встроенные деки — en/de/es/ka с переводом на русский.
3. **Размер сессии**: кап 20 повторений + новые, кнопка «Продолжить» для остатка.
4. **Порядок в сессии**: сначала повторения, потом новые.
5. **Голый текст в чат** (≤ 40 символов, ≤ 3 слов) = «добавить слово». Более длинный текст — поиск незнакомых слов (§4.3, см. решение 13).
6. **Таймзона**: вопрос «сколько у тебя сейчас времени», фиксированное смещение с округлением до 30 минут.
7. **Лимиты Free**: 3 деки, 300 заметок, 10 генераций в день. Действуют только при `PRO_ENABLED=true`.
8. **Режим `recall`**: выключен по умолчанию, включается в настройках деки.
9. **Стартовые деки**: только с переводом на русский. Английский интерфейс на старте без встроенных дек, только свои слова.
10. **Тон**: на «ты». Название бота — задаёт владелец в BotFather, в коде не зашито.
11. **LLM в MVP не используется** (2026-09-04): добавление слов — ручное, генерация карточек перенесена в [M2] (§4.1a).
12. **Отменяет решение 11** (2026-09-06): после прогона `scripts/llm-eval` (9 моделей × 120 кейсов,
    отчёт `results/railway-1/REPORT.md`) генерация возвращена в M1 и включается наличием
    `OPENROUTER_API_KEY`. Модель по умолчанию — `google/gemini-3.7-flash` (≈ $2 на 1000 карточек до
    кэша, judge 19.46/20, грузинский 19.40/20). Без ключа бот ведёт себя ровно как раньше (§4.1).
13. **Слова из текста — в M1** (2026-09-06): раз генерация включена (решение 12), длинный текст
    больше не отвечает «умею только короткие слова»: тем же ключом и той же моделью бот выделяет
    из текста незнакомые слова и показывает чек-лист (§4.3). Бесплатный план — один текст в день;
    карточки из чек-листа тратят обычный лимит генераций.

